---
date: 2026-09-03
last_updated: 2026-09-03T17:49
---

# Log — 2026-09-03

## Day summary
- **Key themes:** RHIVOS release-blocker dashboard finish work — Jira as the single source of truth for release target dates; dropped the self-coined "submission" status (two-status model only); shipped a visible status legend table.
- **Moved forward:** Target dates now pulled live from Jira fixVersion `releaseDate` (config `target_date` now vestigial), rendering "Not set in Jira" when unset; `submission` status removed across banner/brief/config/tests (commit `bcb9166`); status legend table shipped to the Program dashboard; all 4 live docs republished; VROOM-49388 follow-up posted. ruff clean, tests green throughout.
- **Learned:** Source-of-truth over config duplication with explicit gaps (never a silent stale fallback); self-coined categories are unsourced content; check platform capability before honoring a UI-affordance request (Google Docs has no hover tooltips).
- **Open:** Jira-link on the "Needs Attention" column still to build; two FuSa fixVersions (`rhivos-2.0.z`, `rhivos-2.1`) have no `releaseDate` in Jira; RHIVOS 2.0 retro demo to Dana/team not scheduled.

## Decisions

- **Jira is the single source of truth for release target dates (release-blocker agent).** Target dates were previously hand-maintained per-release in `release-config.yaml`; the Core/FuSa split duplicated one date onto both tracks and the values had gone stale. Decided to pull each track's target date live from the Jira **fixVersion `releaseDate`** (`query_jira.resolve_target_date`, reading VROOM project versions via `_get('/project/VROOM/versions')`, lru_cached). Reasoning: a single authoritative source removes drift and stale duplication. Config `target_date` fields are now vestigial (only the standalone render-doc CLI still reads them).
- **No silent config fallback for missing target dates — state it explicitly.** User: "Jira is the most trusted source of truth, if there is no Jira data lets clearly state it in the docs." When a fixVersion has no `releaseDate`, the doc renders **"Not set in Jira"** (`render_doc._target_display`) rather than falling back to a config value. This surfaces the data gap instead of hiding it behind a plausible-but-stale number.
- **Publish reviewable copies before touching live docs.** For the "show me the new G-docs for review" request, temporary `[REVIEW]` copies were published for approval; live docs and the Whitney ping were only done after explicit "continue". Reasoning: outward-facing/irreversible actions get a confirmation gate.
- **Dropped the bespoke "submission" release status — two-status model only (Released, Active).** User questioned where the `submission` status meaning came from. Traced it: I coined it during the Core/FuSa split, extrapolated from one descriptive sentence of Whitney's review ("rhivos-2.0 has not been released. This is the FuSa submission release.") — not from any RHIVOS release-lifecycle taxonomy, and applied to exactly one track (2.0 FuSa). User chose to drop it: use `active` for 2.0 FuSa; the "not released" distinction is already carried by simply *not* being `released`. Fully sourced and simpler. Removed `submission` from `_STATUS_BANNER`, the executive-brief branch, config, and tests. Shipped: commit `bcb9166`, 4 live docs republished (2.0 FuSa now reads "Active"), follow-up note posted on VROOM-49388. ruff clean, 8/8 tests.
- **Status legend: visible table, not a hover tooltip (Google Docs constraint).** User asked for a "legend tooltip" explaining each status. Google Docs does not render HTML hover tooltips — the `title` attribute is stripped on HTML→Docs conversion, and there is no native per-word hover in Docs. Deliverable is a visible color-coded legend table placed under the Program summary table, driven by the same `_STATUS_BANNER` dict as the status cells so labels/colors can never drift. Shipped to the Program dashboard: 9/9 tests, committed, republished live. (Now only two statuses to explain: Released, Active.)

- **Daily consolidation (2026-09-03):** Log rotation — root log count hit 30 (>28); archived the two oldest (`2026-07-02`, `2026-07-05`) to `logs/archive/2026-07/`, tombstoned in `logs/index.md`, appended to the month archive index. Folded today's "live source over duplicated config, with explicit gaps" lesson into the existing `source-authority-hierarchy.md` concept as a specific instance (alongside the 2026-08-02 "source authority over mapping tables" instance) rather than creating a new file — it extends the parent's decision-making power without standing alone. Considered promoting the release-blocker-agent index to Active context "Files" but held it at level 3: though hot today, it was accessed in only 1 of the last 3 active sessions (Sep 3 yes; Sep 2 and Aug 23 no), which fails the "accessed in most sessions" bar for level-4 graduation — gradual promotion, no jumps. No observations at the 2+ threshold to act on (the skill-format migration structure candidate stays deferred — it needs an interactive session for `.claude/skills/` writes); today's three concept candidates remain single-sighting in the journal.

- **Jira link on Needs Attention (Part 2) — SHIPPED Sep 3.** `_needs_attention_jira_url(stats)` builds a Jira issue-navigator URL (`/issues/?jql=key in (...)`, url-encoded) from the actual triage keys (urgent + monitor + active), so the linked set always matches the count; zero renders a plain number. Wired into `_combined_track_rows` (Program dashboard). Also refined the `_bare_vroom` test helper to treat keys inside a tag's attributes (the JQL href) as linked, fixing test failure. ruff clean, 10/10 pytest. Committed `2b7c946` (code-only) + pushed; Program doc republished, links verified live with real triage keys. Scope: Program dashboard only (per request); per-release header cells not linked (available as later consistency add).
- **Summary-table column widths fixed via Docs API (Part 3) — SHIPPED Sep 3, after HTML CSS approach failed.** First attempt: commit `5db4757` added `<colgroup>` with per-column percentages + `white-space: nowrap` on headers + `table-layout: fixed`. Correct HTML, but the **Google Docs HTML importer silently ignores all table CSS** (colgroup/`<col>` widths, per-`<th>` width, `table-layout:fixed`, `white-space:nowrap`). Docs auto-sized the 9 columns to their short *data* content, so wider headers still broke mid-word (Track, Status, RC, Blockers). Key realization: the program page is **already wide** (~750pt usable) — the problem was purely uneven auto-column-sizing, so no orientation change was needed. The reliable lever is the **Google Docs API**: (a) `_SUMMARY_COLUMNS` now carries a **point width** per column (single source; widths sum 715 <= 750pt); (b) `_set_summary_table_column_widths(doc_id)` fetches the doc, locates the summary table by column count == 9 (its start index shifts every run), and `batchUpdate`s `updateTableColumnProperties` with `widthType:FIXED_WIDTH` per column. Wired into `agent.py` right after a successful combined upload (gws only). Because each HTML re-upload wipes native widths, the width step runs after **every** combined upload — not a one-off. ruff clean, 12/12 pytest. Committed `d0de20f` (code-only) + pushed; pipeline rerun (`--all --llm`), verified live: all 9 columns FIXED_WIDTH, sum 715, no mid-word header breaks. Gotcha: the gws command is `gws docs documents get/batchUpdate` (service `docs`, resource `documents`), NOT `gws documents get`.
- **Release H2 headers with "Sum-up Below - Click here for details" (Part 4) — SHIPPED Sep 3.** Each release section header (H2) in the combined dashboard now displays: `{Release} Sum-up Below - Click here for details` (where "Click here for details" links to the per-release doc). When no doc_id, shows just `{Release} Sum-up Below`. ruff clean, 12/12 pytest. Committed `d59b0ac` (code-only) + pushed; combined doc republished, headers verified live.

## Open threads

- **Two FuSa fixVersions still have no `releaseDate` in Jira** (`rhivos-2.0.z`, `rhivos-2.1`) — render "Not set in Jira" until someone sets them.
- **Per-release triage tables (8 columns) have the same column-width issue.** Offered as a follow-up consistency add after the summary table fix; not requested.
- **`.gitignore` gap for `kb/active/**/latest.json`** — the Core/FuSa split introduced per-track subdirs that aren't matched by the top-level `kb/active/*.json` pattern, so they leak into commits. Flagged, not fixed (has CI implications).

## Tasks captured

- The Core/FuSa split work item is complete — no new user-action tasks captured. Two follow-ups noted (belong to others / not requested of the agent): set `releaseDate` in Jira for FuSa versions **rhivos-2.0.z** and **rhivos-2.1** (currently show "Not set in Jira"); schedule the RHIVOS 2.0 retro demo to Dana/team.

## Context

- Completed and shipped the Core/FuSa split for the RHIVOS release-blocker dashboard (Whitney Chadwick review, VROOM-49388), fixing both requested items:
  1. **Core vs FuSa split** — each release renders separate Core (`X.Y.Z-core`) and FuSa (`X.Y.Z`) sections, each with independent filtering, related tickets, stats, chart, Slack digest, and outlook. RHIVOS 2.0 now shows Core 7 active blockers vs FuSa 27 (previously merged).
  2. **Per-track released status** — released status is determined per track, not inherited from `-core`. 2.0 Core reads "Released", 2.0 FuSa reads "FuSa submission (not released)".
- Verification before ship: `ruff` clean; 8/8 pytest pass (added `test_core_and_fusa_render_as_separate_sections`, `test_fusa_2_0_is_not_labelled_released`, `test_missing_target_date_states_not_set_in_jira`); dry runs confirmed the split and the 6-row combined dashboard (3 releases x 2 tracks).
- **Rollout done:** code committed + pushed to `rhivos-release-status` main (commit `e240100`); all 4 live Google Docs republished with per-track Gemini/Vertex outlooks (Program `1kj6C8J...`, 2.0 `1pGTw_...`, 2.0.z `1_cOY9m...`, 2.1 `12AmIot...`); Whitney pinged via comment on VROOM-49388; temporary `[REVIEW]` copies deleted.
- Current live Jira target dates observed: 2.0-core 2026-06-30, 2.0 (FuSa) 2026-10-30, 2.0.z-core 2026-12-15, 2.1-core 2027-06-29; rhivos-2.0.z and rhivos-2.1 (FuSa) have no `releaseDate` → "Not set in Jira".
- **Patches:** Active context "Right now" release-blocker bullet updated (implemented+verified → shipped) during the prior session turns; plan file `core-fusa-split-plan.md` updated with implementation status + target-date decision + shipped section (commit hash + doc IDs).

## Lessons

- When a data field is duplicated across tracks/config and can drift, prefer a single authoritative source (Jira fixVersion `releaseDate`) fetched live over hand-maintained config. When that source is empty, render the gap explicitly ("Not set in Jira") rather than falling back silently — a visible gap prompts a fix; a stale fallback hides one.
- Substring hazard confirmed again: `rhivos-2.0` is a substring of both `rhivos-2.0-core` and `rhivos-2.0.z`. Track partitioning must use exact set membership (JQL `fixVersion IN`), never `startswith`/`in`. Kept the whole split on exact matching.
- **Self-coined terminology is a liability — flag and remove it.** The `submission` status was a term I invented from a source's *descriptive* sentence and then reified into a named category applied to one track. The tell that something was wrong: writing a legend entry for it felt off, because there was no authoritative definition to cite (bumps Rule 23 "source terminology only" and Rule 10 "no unsourced content"). When a status/category has no authoritative source, don't coin one to fill the gap — express the distinction with what's already sourced (here: "not `released`" needs no third status). Good user catch.
- **Google Docs HTML importer silently ignores all table CSS.** The HTML → Google Docs converter strips `<colgroup>`/`<col>` width attributes, per-`<th>` width styles, `table-layout:fixed`, and `white-space:nowrap` — it auto-sizes table columns to their short data content regardless of HTML markup. This caused summary-table header words to break mid-word even with correct HTML (commit `5db4757`). The only reliable lever for table formatting is the **Google Docs API** post-upload: `updateTableColumnProperties` with `FIXED_WIDTH` widths in points. The page geometry and total usable width (~750pt for the program page) are preserved from HTML; only column-level sizing must be applied via API. Extends the family of Docs HTML-importer limitations (anchor links stripped 2026-06-24, dateElement smart chips invisible 2026-08-02, container table for uniform width 2026-06-24).
- **Per-track state files leak into commits — `.gitignore` gap.** `.gitignore` ignores `kb/active/*.json` (top level only), but the Core/FuSa split introduced per-track subdirs `kb/active/<release>/<track>/latest.json` which are not matched. They update on every run and got swept into the legend commit by `git add -A`. Cleanup item: widen the ignore pattern (has CI implications) rather than fixing silently.

## System observations

- **Concept candidate:** "Source-of-truth over config duplication + explicit gaps" — when a value is both derivable from an authoritative external system and hand-copied into config, drive display from the external system and render an explicit "not set" state when it is absent, instead of a silent config fallback. Generalizes beyond this agent.
- **Concept candidate:** "Self-coined categories are unsourced content" — when a synthesis reifies a source's descriptive phrase into a named category (status/type/label), that category has no authoritative definition and drifts into unsourced content. Tell: you can't write a definition for it without inventing one. Prefer expressing the distinction with already-sourced primitives. Reinforces Rules 10 and 23. (seen: 1)
- **Concept candidate:** "Platform capability check before honoring a UI request" — a "tooltip" request assumes hover support the target medium (Google Docs) doesn't have (HTML `title` stripped on conversion). Confirm the rendering platform supports the requested affordance before designing to it; offer the closest supported equivalent (visible legend). Ties to existing Google-Docs-conversion-limits observations (anchor links stripped, smart chips invisible).
- **Concept candidate:** "Google Docs HTML importer ignores table CSS" — the HTML → Google Docs converter silently strips all table-level CSS (`<colgroup>`/`<col>` widths, per-`<th>` width styles, `table-layout:fixed`, `white-space:nowrap`), auto-sizing columns to their short data content regardless of HTML markup. The only reliable formatting lever is the **Google Docs API** post-upload (`updateTableColumnProperties` with `FIXED_WIDTH` in points). Page geometry and usable width are preserved from HTML; only column-level sizing must be applied via API. When building HTML reports for Google Docs conversion, table formatting requires a two-step flow: (1) HTML upload, (2) Docs API batchUpdate with column widths. Extends the family of Docs HTML-importer limitations (anchor links, dateElement smart chips, container table pattern). (seen: 1)
