---
date: 2026-08-03
last_updated: 2026-08-03T12:15
---

# Log - 2026-08-03

## Day summary
- **Key themes:** GitLab CI/CD pipeline debugging (Podman executor, runner config), code quality (56 ruff lint fixes), full pipeline operational (all 3 stages), shared runners migration
- **Moved forward:** Pipeline fully green end-to-end on shared Kubernetes runners. All data sources working (Jira, Slack, meeting notes). LLM synthesis via Gemini Vertex AI operational. 4 Google Docs auto-updating. Scheduled pipeline (weekdays 07:00 UTC) no longer depends on laptop.
- **Learned:** Podman-as-Docker executor via socket, system vs user runner config separation, TOML multi-value ambiguity, DOCKER_AUTH_CONFIG for private image pulls, GCP API enablement per project, Google Docs sharing with service accounts
- **Open:** Demo to Dana/team not yet scheduled

## Decisions
- Used rootful Podman socket (Option 1 over running runner as user) for GitLab runner - more robust, survives reboots as a system service. Enabled via `sudo systemctl enable --now podman.socket`, creates `/run/podman/podman.sock`.
- Switched build image from `quay.io/buildah/buildah:latest` (requires auth) to `registry.access.redhat.com/ubi9/buildah` (public, no auth needed for pulls).
- Copied user-level runner config (`~/.gitlab-runner/config.toml`) to system config (`/etc/gitlab-runner/config.toml`) since the system-level gitlab-runner process was running with an empty config.

## Tasks captured
- quay.io auth resolved via DOCKER_AUTH_CONFIG CI variable (repo stays private).
- Shared runners migration completed (all jobs tagged `itup-alm-x86`).

## Ideas
- None.

## Context
- **GitLab CI/CD pipeline debugging (session 12).** Continued from yesterday's session (9005257f). Pipeline #16798588 had failed overnight because the Docker executor couldn't connect to `/var/run/docker.sock` - user runs Podman, not Docker.
- **Runner config confusion.** The runner registration yesterday wrote to `~/.gitlab-runner/config.toml` (user config), but the system-level `gitlab-runner` process (PID 1514) used `/etc/gitlab-runner/config.toml` (which was empty). The job only ran yesterday because there was likely a user-level runner process that has since stopped.
- **Duplicate host line fix.** An earlier edit to the user config added `host = "unix:///run/user/100795/podman/podman.sock"` (rootless), then the sed command added `host = "unix:///run/podman/podman.sock"` (rootful). Had to remove the rootless line since the system runner runs as `gitlab-runner` user which can't access the rootless socket.
- **Lint errors fixed.** 56 ruff lint errors in the release blocker agent code - 55 auto-fixable (import sorting), 14 manual fixes (BLE001 blind exceptions, PLW1510 subprocess.run without check, S110 try/except/pass). Pipeline went fully green after fixes.
- **Shared runners discovered.** GitLab instance has shared Kubernetes runners available: 5x x86 (`itup-alm-x86` tag), 1x ARM (`itup-alm-arm`), 1x test (`itup-alm-x86-test`). All require explicit `tags:` in CI config (`run_untagged: false`). User considering switching to avoid dependency on local laptop.
- **run-daily job failure and resolution.** Scheduled run-daily job (#57699215) failed because rootful Podman couldn't pull `quay.io/aefrat/rhivos-release-status:latest` without credentials. User chose `DOCKER_AUTH_CONFIG` CI variable over making quay.io repo public (public means anyone on the internet, not just Red Hat). First attempt had wrong base64 encoding (wrong user:token values), second worked. Image pull fixed.
- **Google Docs API not enabled.** Agent crashed on `HttpError 403: Google Docs API has not been used in project 530839756563`. GCP project needed Google Docs API enabled. Also fixed: `HttpError` not caught by existing exception handler (only `OSError, ValueError, KeyError` were caught). Added `_FETCH_ERRORS` tuple pattern with conditional `HttpError` inclusion.
- **LLM backend mismatch.** CI used `--llm-backend vertex` which calls Claude via Vertex AI (AnthropicVertex), but `claude-haiku-4-5-20251001` was not available in the GCP project/region. Switched to `--llm-backend gemini` (Gemini 2.5 Flash via Vertex AI, already enabled). Also added catch-all for unexpected API errors in `_call_vertex`.
- **Meeting notes permission.** GCP service account needed Viewer access to the RR meeting notes Google Doc. After sharing, all 107 meetings parsed successfully.
- **Pipeline source mismatch.** `glab ci run` creates pipelines with `source: api`, not `source: web`. run-daily rules only matched `schedule` and `web`. Added `api` to the rules.
- **Shared runners migration.** Discovered 5 online shared Kubernetes runners (`itup-alm-x86` tag) on gitlab.cee.redhat.com. Added `tags: [itup-alm-x86]` to all 3 jobs. Full pipeline passed: lint (9s), build (104s), run-daily (56s). Pipeline now runs without laptop dependency.

## Lessons
- GitLab runner executor config is on the host machine (`config.toml`), not configurable via GitLab UI. Registration location (user vs system) depends on how `gitlab-runner register` was invoked (with or without sudo).
- Podman's Docker-compatible socket (`podman.socket`) enables GitLab's Docker executor to work with Podman without changing executor type. Rootful socket needed when runner runs as system service.
- Multiple `host =` lines in TOML `[runners.docker]` section - last value wins in some parsers, but it's ambiguous. Always ensure only one.
- `DOCKER_AUTH_CONFIG` CI variable provides runner-level auth for image pulls from private registries. Value format: `{"auths":{"registry":{"auth":"base64(user:token)"}}}`. Must use `echo -n` (not `echo`) for base64 to avoid trailing newline.
- `glab ci run` triggers with `source: api`, not `source: web`. CI rules matching only `web` won't see API-triggered pipelines.
- GCP project API enablement is per-API: Vertex AI enabled doesn't mean Google Docs API is enabled. Each must be explicitly activated.
- Claude on Vertex AI (AnthropicVertex) requires model availability in the GCP project/region via Model Garden. Gemini is available by default with Vertex AI enabled.
- quay.io "public" means internet-public, not org-scoped. No "Red Hat internal only" visibility option exists on quay.io.

## Open threads
- **Demo to Dana/team.** Pipeline fully operational, need to schedule the demo.
- **Local runner cleanup.** Local Podman runner still registered and active. Could disable or remove if shared runners prove reliable.

## System observations
- **Concept candidate:** "Incremental CI pipeline stabilization" - 9 fix-retry cycles to reach full green, each stage revealing the next failure. Complexity is in the number of layers, not the difficulty of any fix.
- **Concept candidate:** "DOCKER_AUTH_CONFIG for private registry pulls" - Docker config.json format CI variable for authenticating image pulls without making repos public.
- **Concept candidate:** "GitLab pipeline source determines job visibility" - different trigger methods produce different `CI_PIPELINE_SOURCE` values; CI rules must match the correct source.

## Patches
- CLAUDE.md Active context: release blocker agent pipeline status updated from "run-daily blocked on quay.io auth" to "fully operational on shared runners" (patched via sessionStart hook earlier).

## Daily consolidation (maintenance)
- Active context day counts updated (Hubert 34, Aman 17, Roderick 64, Workday evals 24d overdue, Meital 14d, RHAS QE deliverables 32d, PDR calibration 11d).
- Release blocker agent pipeline status updated: lint/build green, run-daily blocked on quay.io auth.
- Deferred queue refreshed with current counts. Paul Wallrabe reply entry preserved from earlier session.
- Methodology daily capture: 2 tasks classified (enhanced/tool-building, assisted/tool-building). 1 anti-pattern (duplicate TOML host line). 1 positive pattern (multi-layer pipeline recovery).
- No concepts created (all new observations at seen: 1).
- No mature observations to act on.
- No log rotation needed (26 files, under 28 limit).
- No file promotions or demotions (no hot files, no sustained high-access patterns).
