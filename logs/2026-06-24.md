---
date: 2026-06-24
last_updated: 2026-06-24T17:30
---

# Log — 2026-06-24

## Decisions

- **Slack canvas publishing abandoned.** Investigated pushing PitCrew HTML report to Slack canvas (F0BCKHJATC2) via markdown conversion. All canvas API endpoints (`canvases.edit`, `canvases.access.set`, `canvases.sections.lookup`) return `not_allowed_token_type` with xoxc user tokens. Canvas API requires `xoxp` (user OAuth) or `xoxb` (bot) tokens with `canvases:write` scope — neither available. User also declined the `files.upload` alternative ("No thanks, I do not want to upload anything directly to the channel"). **Result:** Slack remains strictly read-only — no posting, no uploads, no canvas writes.

## Tasks captured

_None._

## Ideas

_None._

## Context

- **LP status report (cron-triggered, Jun 24).** Ran RHIVOS QC LP daily status report. 3 changes from Jun 23 baseline:
  1. **VROOM-42116:** In Progress → Review — Matt Goldman submitted MR #149 for CDN directory path in `cdn-definitions-private`. The blocker that was waiting on Petr Sabata approval is now progressing.
  2. **VROOM-44910:** NEW — Create QC variant for RHIVOS Core/FuSa LP (Ozan Unsal). Compose work is now ticketed.
  3. **VROOM-44911:** NEW — Create brew tags for QC packages (Ozan Unsal). Prerequisite for compose work.
  - Overall status remains At Risk: 6 distribution tasks still unassigned, CDN path MR not yet merged, ~5 weeks to August target.
  - Report emailed, stores updated, committed as `26704eb`.

## Lessons

_None._

## Open threads

- **VROOM-42116 CDN path MR** — submitted but not yet merged. Monitor for approval.
- **Slack canvas API** — dead end with current tokens. Would need a Slack bot app with `canvases:write` scope if ever revisited.

---

## Decisions (session 2)

- **Switched Slack access from bash curl to community slack-mcp MCP server.** User requested setup per RH AI Skills Workshop (module 12). Chose the community server (`redhat-community-ai-tools/slack-mcp`) over the official Slack MCP plugin (`mcp.slack.com` OAuth) because it uses the same xoxc/xoxd browser tokens already in `~/.bashrc` — no OAuth dance. Server runs in read-only mode (`--read-only`). Setup: cloned to `~/slack-mcp`, Python venv, wrapper script (`run-slack-mcp.sh`) maps `SLACK_XOXD_COOKIE` → `SLACK_XOXD_TOKEN`, configured in `~/.mcp.json`.
- **Removed official Slack MCP plugin** from `~/.claude/settings.json` (`slack@claude-plugins-official`). Only the community slack-mcp remains.
- **Created global Claude Code rule** (`~/.claude/CLAUDE.md`) enforcing community slack-mcp as the only Slack access method across all projects.
- **Updated CLAUDE.md rule 20** from "use bash curl" to "use community slack-mcp MCP server only." Also updated auto-memory (`feedback_slack-bash-not-mcp.md`) and `MEMORY.md` index to match.

## Context (session 2)

- **Slack access method evolution:** OAuth MCP (rejected day 1) → bash curl (adopted Jun 23, fast-tracked as Rule 20) → community slack-mcp MCP (adopted Jun 24). The community MCP server converges both approaches: browser tokens for auth reliability + MCP tool calls for structured access. Read-only constraint preserved throughout.
- **Env var mismatch required wrapper script.** User's `~/.bashrc` exports `SLACK_XOXD_COOKIE`; the slack-mcp server expects `SLACK_XOXD_TOKEN`. Created `~/slack-mcp/run-slack-mcp.sh` to source bashrc and remap.
- **MCP server not yet tested in live Claude Code session.** Server initializes correctly via direct stdio test (returns 10 read-only tools). Needs session restart to load `~/.mcp.json`. Test against #team-auto-toolchain (C04JDFLHJN6) was interrupted.

## System observations (session 2)

- **Rule candidate (fast-tracked):** "Use community slack-mcp MCP server for all Slack access." Direct user instruction, applied immediately to CLAUDE.md Rule 20, global `~/.claude/CLAUDE.md`, and auto-memory. This supersedes the Jun 23 fast-track of "bash curl only." (seen: 1, fast-tracked)
- **Concept candidate:** "Env var naming conventions across tools" — when a tool expects `SLACK_XOXD_TOKEN` but the ecosystem uses `SLACK_XOXD_COOKIE`, a thin wrapper script is the cleanest bridge. Avoids modifying either side. Generalizable to any MCP server that expects differently-named env vars. (seen: 1)
