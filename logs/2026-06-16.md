---
date: 2026-06-16
last_updated: 2026-06-16T09:45
---

# Log — 2026-06-16

## Context

- **RC3 go/no-go day.** Today is the Monday decision point for RC3.
- **Reviewed Brew permissions from yesterday's audit.** User requested a comparison of Sameera Kalgudi (`skalgudi`) and Ozan Unsal (`ounsal`) permissions from `brew-confs/granted.yaml`. Both have broad `tagger`; Sameera additionally has `package-list-admin` (org-wide), Ozan has `pkglist-rhivos` + `tag` + `target` (RHIVOS-scoped + structural). Neither has `rhivos-tagger` — only the `auto-toolchain-service-brew` service account does.
- **Explained each Brew permission in detail:** `tagger` (tag/untag builds), `package-list-admin` (manage whitelists org-wide), `pkglist-rhivos` (manage whitelists for `rhivos-*` tags only), `tag` (create/edit/delete tag structures), `target` (create/edit/delete build targets).
- **Created package tagging workflow visualization** for team presentation:
  `Build (rhpkg/Brew) → -gate → Gator (Greenwave eval) → -candidate → Signing (ET) → Errata → Compose (ODCS) → -pending → CDN`
  Sources: `rhivos-release-approach.md:182-195`, Confluence package workflow page, `gator.md`, ATC Distribution FA wiki.
- **Reran daily manager report** manually with `source ~/.bashrc` — generated and emailed successfully.
- **Diagnosed Channel Digest "Not logged in" bug** in daily manager report. Root cause: cron job runs `claude -p` without Vertex AI env vars (`CLAUDE_CODE_USE_VERTEX`, `ANTHROPIC_VERTEX_PROJECT_ID`) set in `~/.bashrc`. Cron doesn't source `.bashrc`, so `claude -p` returns "Not logged in · Please run /login" which the script renders verbatim as the digest content. Confirmed by testing `env -i HOME=$HOME PATH=$PATH claude -p "say ok"` → reproduces the login error. Started a fix (auth error detection in `generate_report.py`) but user asked to revert — changes reverted cleanly.
- **User correction:** Sameera Kalgudi is male (he/him), not female.

## Decisions

- None — review and operational session.

## Tasks captured

- None new.

## Open threads

- **Manager report `claude -p` auth in cron** — root cause identified (missing env vars), fix reverted at user request. Still needs resolution: either source `.bashrc` in the cron entry, or export the Vertex vars directly in the crontab, or add auth error detection to the script. The report runs but all AI-generated sections (Summary, Channel Digest) show "Not logged in" when run from cron.

## System observations

- **Rule candidate:** Sameera Kalgudi is male — when referencing team members, don't assume gender from names. Ask or use neutral pronouns when unsure. (User explicit correction.)
